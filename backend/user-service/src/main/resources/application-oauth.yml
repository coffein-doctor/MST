# kakao oauth2 설정

spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 4c2e4d4852cafb55e5d18db0521ecee3
            redirect-uri: {baseUrl}/oauth2/callback/{registrationId} #http://localhost:8081/oauth2/callback/kakao
            client-authentication-method: POST
            client-secret: NkzavTQYRPwGRFgbWHTtLxc7Zsr2Ncuf
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname
              - profile_image
              - account_email
            client_name: kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id


#jwt:
#  token:
#    secret-key: chadoll
#  access-token:
#    expire-length: 1800000
#  refresh-token:
#    expire-length: 1209600000