server:
  port: 8082

spring:
  application:
    name: beverage-service

# 유레카 서버 설정
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:1234mst@localhost:8761/eureka